///
/// FlixAPI Framework Entry Point
///
/// This is a minimal entry point for the FlixAPI framework.
/// For complete examples, see the `examples/` directory.
///

def main(): Unit \ IO = {
    println("FlixAPI Framework")
}
